pipeline {
    agent any
    triggers {
        // eventTrigger event(generic('eventTrigger'))
        eventTrigger simpleMatch('eventTrigger')
    }
    stages {
        stage('Trigger') {
            steps {
                echo 'Received eventTrigger'
            }
        }
    }
}
